
Done some

1. Don't use the logic of iterating through the months as per existing model; a JSON array would be just fine
2. You might remove the js on top since not using for this page; left it there to see the model of js
3. on ajax calls please use plain db queries (no ORMs)
